# Engineering Guidelines
- Backend strictly uses FastAPI, Pydantic, and LangGraph.
- Frontend strictly uses Next.js App Router and Tailwind.
- Keep dependencies minimal to ensure a functional, local MVP.
- Do not commit secrets to code.
- Write tests for core endpoints like `/health`.
